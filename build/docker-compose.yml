version: '3'

services:
  web:
    build:
      context: docker/nginx
    volumes:
      - ../:/app:cached

  node:
    image: node:latest
    volumes:
      - ../:/app/:cached
    command:
      - sh
      - /app/build/node_entrypoint.sh
    expose:
      - "80"
      - "3001"

